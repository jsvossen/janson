<% if controller_name == "posts" && (action_name == "index" || action_name == "create") %>
	<%= post.body_trimmed %>
	<%= link_to("Read more", post) if post.body.size > post.body_trimmed.size %>
<% else %>
	<%= post.body %>
<% end %>
<%= image_tag @post.picture.url(:medium) if post.picture? %>